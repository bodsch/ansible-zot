---

- name: flush handlers
  ansible.builtin.meta: flush_handlers

- name: ensure zot is enabled on boot
  when:
    - not running_in_check_mode
  become: true
  ansible.builtin.service:
    name: zot
    enabled: true
    state: started

...
